using System.ComponentModel;

namespace Bank.Domains.Payment
{
    /// <summary>
    /// 支付通道
    /// </summary>
    public enum ChannelEnum
    {
        ICBC = 1,
        ABC,
        BOC,
        CCB
    }
}
